import assert from 'node:assert/strict';import {reconcileReadOnly} from './recovery-service.mjs';let writes=0;const j={youtube_video_id:'Y',planned:{title:'ABC',description:'D',categoryId:'10',privacyStatus:'private',publishAt:'T',channelId:'C'}};const adapt=async v=>{const r=await reconcileReadOnly({journalRow:j,readRemote:async()=>v});return JSON.parse(JSON.stringify({ok:true,status:r.state==='REMOTE_STATE_VERIFIED'?'VERIFIED':'RECONCILIATION_REQUIRED',firstReadPassed:!!r.first?.pass,secondReadPassed:!!r.second?.pass,fields:[...(r.first?.mismatches||[])]}))};const ok=await adapt(j.planned);assert.equal(ok.status,'VERIFIED');const bad=await adapt({...j.planned,title:'XYZ'});assert.equal(bad.status,'RECONCILIATION_REQUIRED');assert.equal(writes,0);console.log('renderer IPC serialization tests passed');
