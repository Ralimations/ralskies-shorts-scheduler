import assert from 'node:assert/strict';import {classifyTitle,reviewPriority} from './title-intelligence.mjs';const row={source_song:'Black Sorrow',artist_or_fandom:'Alien Stage'};assert.equal(classifyTitle('Black Sorrow | Male Cover',row).health,'TITLE_HEALTHY');assert.equal(classifyTitle('this moment here',row).health,'TITLE_REVIEW_RECOMMENDED');assert.equal(classifyTitle('',row).health,'TITLE_HIGH_PRIORITY_REVIEW');assert.equal(reviewPriority({...row,status:'SCHEDULED'}),'PRIORITY_1');assert.equal(reviewPriority({...row,status:'BATCH_READY',scheduled_date:1}),'PRIORITY_2');console.log('calibration tests passed');