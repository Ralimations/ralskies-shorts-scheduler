import assert from 'node:assert/strict';import {classifyDuplicate} from './duplicate-model.mjs';const a={short_id:'A',file_hash:'x',status:'BATCH_READY',batch_id:'B'};const d={short_id:'A',file_hash:'x',status:'DUPLICATE'};assert.equal(classifyDuplicate(a,[a,d]).kind,'CANONICAL_ACTIVE');assert.equal(classifyDuplicate(d,[a,d]).kind,'RECORD_DUPLICATE');assert.equal(classifyDuplicate({...a,status:'CONTENT_BLOCKED'},[a,d]).kind,'CONTENT_BLOCKED');console.log('duplicate model tests passed');
