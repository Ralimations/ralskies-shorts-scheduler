import fs from 'node:fs/promises';
import path from 'node:path';
const ROOT=path.resolve(import.meta.dirname,'..');
async function readJson(file){return JSON.parse(await fs.readFile(file,'utf8'));}
async function accessToken(config){const token=await readJson(path.resolve(ROOT,config.oauthTokenPath));if(token.expires_at&&Date.now()<token.expires_at-60000)return token.access_token;const clientFile=await readJson(path.resolve(ROOT,config.oauthClientPath));const client=clientFile.installed||clientFile.web;const response=await fetch('https://oauth2.googleapis.com/token',{method:'POST',body:new URLSearchParams({client_id:client.client_id,client_secret:client.client_secret,refresh_token:token.refresh_token,grant_type:'refresh_token'})});const body=await response.json();if(!response.ok)throw Error(`OAuth refresh failed: ${body.error||body.error_description||response.status}`);const fresh={...token,access_token:body.access_token,expires_at:Date.now()+body.expires_in*1000};await fs.writeFile(path.resolve(ROOT,config.oauthTokenPath),JSON.stringify(fresh,null,2));return fresh.access_token;}
async function api(config,url,options={}){const response=await fetch(url,{...options,headers:{...(options.headers||{}),Authorization:`Bearer ${await accessToken(config)}`,'Content-Type':'application/json'}});const text=await response.text();let body={};try{body=text?JSON.parse(text):{};}catch{}if(!response.ok)throw Error(`YouTube API ${response.status}: ${body.error?.message||text.slice(0,300)}`);return body;}
export function createYouTubeRealClient({config}={}){if(!config)throw Error('YOUTUBE_CONFIG_REQUIRED');return {videos:{async update(payload){return api(config,'https://www.googleapis.com/youtube/v3/videos?part=snippet%2Cstatus',{method:'PUT',body:JSON.stringify(payload)});},async list(id){const q=new URLSearchParams({part:'id,snippet,status,processingDetails',id});const body=await api(config,`https://www.googleapis.com/youtube/v3/videos?${q}`);return body.items?.[0]||null;}}};}
export async function loadYouTubeConfig(){const cfg=await readJson(path.join(ROOT,'phase2','config.json'));return {...cfg,oauthClientPath:path.resolve(ROOT,cfg.oauthClientPath),oauthTokenPath:path.resolve(ROOT,cfg.oauthTokenPath)};}