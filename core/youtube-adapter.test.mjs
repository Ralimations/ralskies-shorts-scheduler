import assert from 'node:assert/strict';import {createRealAdapter,armRealPlan,disarmRealPlan} from './youtube-adapter.mjs';const plan={executionId:'E',planHash:'H',batchId:'BULK_02',operationCount:1,youtubeIds:['Y']};let calls=[];const fake={videos:{update:async p=>{calls.push(p);return {ok:true}}}};const op={executionContext:{...plan,approved:true,plan: {approvedRowIds:['R'],youtubeIds:['Y']}},executionRecord:{executionId:'E'},rowJournal:{state:'APPLYING',executionId:'E',youtube_video_id:'Y'},row:{short_id:'R',youtube_video_id:'Y'},youtubeId:'Y',snippet:{title:'T',description:'D',tags:['a'],categoryId:'10'},status:{privacyStatus:'private',publishAt:'T'}};disarmRealPlan();await assert.rejects(()=>createRealAdapter({enabled:true,fakeClient:fake}).applyVideoUpdate(op),/DISABLED/);armRealPlan(plan);await createRealAdapter({enabled:true,fakeClient:fake}).applyVideoUpdate(op,{...op.executionContext,approved:true,persistenceVerified:true,durableState:'APPLYING',executionRecord:op.executionRecord,rowJournal:op.rowJournal,shortId:'R',youtubeId:'Y'});assert.equal(calls.length,1);assert.equal(calls[0].id,'Y');assert.deepEqual(calls[0].snippet.tags,['a']);await assert.rejects(()=>createRealAdapter({enabled:true,fakeClient:fake}).applyVideoUpdate(op,{...op.executionContext,executionId:'BAD',approved:true,persistenceVerified:true,durableState:'APPLYING',executionRecord:op.executionRecord,rowJournal:op.rowJournal,shortId:'R',youtubeId:'Y'}),/NOT_ARMED/);console.log('youtube adapter armed-branch tests passed');
