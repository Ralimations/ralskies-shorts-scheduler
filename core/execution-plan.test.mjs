import assert from 'node:assert/strict'; import {createExecutionPlan,assertExecutionPlanCurrent,assertWriteAllowed} from './execution-plan.mjs';
const rows=[{short_id:'A',youtube_video_id:'1'},{short_id:'B',youtube_video_id:'2'},{short_id:'C',youtube_video_id:'3'}]; const p=createExecutionPlan({batchId:'BULK_02',mode:'CANARY',rowIds:['A','B'],rows}); assert.equal(p.operationCount,2); assert.throws(()=>assertWriteAllowed(p,rows[2]),/WRITE GUARD/); assert.throws(()=>createExecutionPlan({batchId:'BULK_02',mode:'CANARY',rowIds:['A'],rows}),/exactly 2/); assert.throws(()=>createExecutionPlan({batchId:'BULK_02',mode:'SELECTED',rowIds:['A','A'],rows}),/duplicate/); console.log('execution-plan tests passed');
const currentRows=[{short_id:'A',batch_id:'BULK_03',youtube_video_id:'1',status:'BATCH_READY',schedule_eligible:'YES',public_title:'Title',description:'Description',youtube_tags:'one, two',youtube_category_id:'10',scheduled_date:'2026-09-14',scheduled_time:'17:30',file_hash:'A'.repeat(64)}];const currentPlan=createExecutionPlan({batchId:'BULK_03',mode:'PRODUCTION',rowIds:['A'],rows:currentRows});assert.equal(assertExecutionPlanCurrent(currentPlan,currentRows),true);assert.throws(()=>assertExecutionPlanCurrent(currentPlan,[{...currentRows[0],public_title:'Changed'}]),/TARGETS_STALE/);
