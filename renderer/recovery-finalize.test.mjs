import assert from 'node:assert/strict';import fs from 'node:fs/promises';const s=await fs.readFile(new URL('./recovery-finalize.js',import.meta.url),'utf8');assert(s.includes('data-finalize-recovery'));assert(s.includes('FINALIZING TRACKER...'));assert(s.includes('RECOVERY COMPLETE'));assert(s.includes('ALREADY_COMPLETE'));console.log('recovery finalize DOM module tests passed');
